# Minimal requirements for Azure App Service
# ML dependencies excluded since we serve pre-computed predictions

# Core
flask>=2.3.0
flask-cors>=4.0.0
gunicorn>=21.0.0

# Data Processing (minimal)
pandas>=2.0.0
numpy>=1.24.0
